language: java
sudo: true

cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/

jobs:
  include:
    - stage: build with gradle & generate reports
      script:
      - ./gradlew clean build jacocoTestReport coveralls
