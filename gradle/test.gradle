buildscript{
    ext {
        restAssuredVersion = '3.1.0'
    }
    repositories(repos)
}

sourceSets {
    test.groovy.srcDirs(
        "src/test/unit",
        "src/test/functional",
        "src/test/commons"
    )
}

idea {
    module {
        test
    }
}
